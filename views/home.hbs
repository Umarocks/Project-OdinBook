<!DOCTYPE html>
<html>

<head>
    <title>Blog Home</title>
</head>

<body>
    <h1>Welcome to the Blog</h1>

    {{!-- {{#if posts.length}}
    <ul>
        {{#each posts}}
        <li>
            <h2>{{title}}</h2>
            <p>{{content}}</p>
            <p>Author: {{author}}</p>
            <p>Date: {{formatDate createdAt}}</p>
        </li>
        {{/each}}
    </ul>
    {{else}}
    <p>No blog posts available.</p>
    {{/if}} --}}

<!-- In your Handlebars template file (.hbs) -->
<a href="/auth/login">
    <button>Login</button>
</a>


</body>

</html>